extends base

block content
    h2 #{pageTitle}
    form(method='post')
        input(name='title', placeholder='Title', required)
        input(name='description', placeholder='Description', required)
        input(name='hashtags', placeholder='Hashtags, separated by ","', required)
        input(type='submit', value='Upload')