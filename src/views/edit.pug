extends base

block content
    h3 Change title of Video
    form(method="post")
        input(type="text", name="title", value=video.title, placeholder="Video Title", required)
        input(type="submit", value="Save")  